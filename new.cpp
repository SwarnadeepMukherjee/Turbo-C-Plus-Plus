#include<iostream>
#include<conio.h>
void main()
 {   
        {
            int a;
            int b;
            int result;
            char op;
            cout<<"enter the first number";
            cin>>a;
            cout<<"enter the second number";
            cin>>b;
        }
        if (op=+):
        {
            result=(a+b);
            cout<<"the answer is :"<<result<<"/n";
        }
        else if (op=-):
        {
            result=(a-b);
            cout<<"the answer is :"<<result<<"/n";
        }
        else if (op=*):
        {
            result=(a*b);
            cout<<"the answer is :"<<result<<"/n";
        }
        else (op=/):
        {
            result=(a/b);
            cout<<"the answer is :"<<result<<"/n";
        }
    getch();
 }